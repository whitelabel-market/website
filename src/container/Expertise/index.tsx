import React from 'react';
import Section from '@/components/Section';

export default function Expertise() {
  return (
    <Section
      color={`yellow`}
      id={`expertise`}
      tag={`Expertise`}
      title={`Building the digital economy of tomorrow`}
      description={`We invest in the most innovative ideas that address unmet market needs in finance and technology. We are particularly interested in distributed ledger technologies which enable innovation across a wide range of industries.`}
    />
  );
}
